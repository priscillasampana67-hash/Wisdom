version: '3.8'
services:
  farmvoice-ai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./static/audio:/app/static/audio
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
